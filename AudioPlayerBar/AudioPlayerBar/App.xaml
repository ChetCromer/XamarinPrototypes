<?xml version="1.0" encoding="utf-8"?>
<Application xmlns="http://xamarin.com/schemas/2014/forms" 
		xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
		xmlns:local="clr-namespace:AudioPlayerBar;assembly=AudioPlayerBar" 
		x:Class="AudioPlayerBar.App">
	<Application.Resources>
		<ResourceDictionary>
			<ControlTemplate x:Key="PlayerPageTemplate">
				<StackLayout VerticalOptions="FillAndExpand">
					<ContentView VerticalOptions="FillAndExpand">
						<ContentPresenter />
					</ContentView>
					<StackLayout x:Name="stackFooterContent" IsVisible="{x:Static local:AudioPlayer.Current.IsLoaded}">
						<Label Text="IsPlaying Value:" FontSize="24" />
						<Label text="{x:Static local:AudioPlayer.Current.IsPlaying}" />
						<Button Text="Toggle IsPlaying" Clicked="Click_PlayPause" />
					</StackLayout>
				</StackLayout>
			</ControlTemplate>
		</ResourceDictionary>
	</Application.Resources>
</Application>